'use strict'
const allStudents = ['Иванов', 'Петров', 'Сидоров', 'Кузнецов', 'Смирнов', 'Попов', 'Соколов'];
const failedStudents = ['Сидоров', 'Смирнов', 'Попов'];

const filter = allStudents.filter(allStudents => !failedStudents.includes(allStudents));
console.log(filter);

